Lazy Loaded modules can be created using the Code Splitting Pattern:

```bash
ng g module [NAME] --route [ROUTE] --module [PARENT-MODULE]
```

There is a tutorial in `tutorials/code-splitting`. It also contains `bundle preloading`.