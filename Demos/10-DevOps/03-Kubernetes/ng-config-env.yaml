apiVersion: apps/v1
kind: Deployment
metadata:
  name: ng-config-env
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ng-config-env
  template:
    metadata:
      labels:
        app: ng-config-env                 
    spec:
      containers:
      - name: ng-config-env
        image: arambazamba/ng-config-env
        imagePullPolicy: Always
        ports:
        - containerPort: 80        
        resources:
          limits:            
            cpu: "500m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ng-config-env-lb
spec:
  type: LoadBalancer
  ports:
  - port: 8081
    targetPort: 80
  selector:
    app: ng-config-env
---
